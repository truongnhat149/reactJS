// Declection function

function sum(n1, n2) {
    return n1 + n2;
}

// Expression function

const subtract = function(n1, n2) {
    return n1 - n2;
}

// Anonymus function

let result = number.reduce(function(total, number) {
    return total + number;
})

// khai báo 1 class
function Student(fullName, age) {
    this.fullName = fullName;
    this.age = age;
    // khai báo 1 phương thức trong class
    this.info = function () {

    }
}

